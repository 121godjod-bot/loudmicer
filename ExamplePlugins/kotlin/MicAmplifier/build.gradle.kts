version = "1.0.0"
description = "Amplifies microphone input with gain and EQ"

aliucord {
    changelog.set("""
        # 1.0.0
        * Initial release
        * Microphone amplification with equalizer
    """.trimIndent())
}
